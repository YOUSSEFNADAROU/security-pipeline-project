name: DAST Pipeline - Juice Shop

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  dast-scan:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🔧 Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: 📂 Install Juice Shop
        run: |
          git clone https://github.com/OWASP/juice-shop.git
          cd juice-shop
          npm ci

      - name: 🚀 Launch Juice Shop
        run: |
          cd juice-shop
          nohup npm start &   # démarre Juice Shop en arrière-plan
          sleep 30            # attendre le démarrage
          if curl -s http://localhost:3000 > /dev/null; then
            echo "Juice Shop opérationnel !"
          else
            echo "⚠️ Problème avec Juice Shop"
            exit 1
          fi

      - name: 🐳 Pull OWASP ZAP
        run: docker pull owasp/zap2docker-stable

      - name: 🎯 Run DAST Scan
        run: |
          docker run --rm --network host owasp/zap2docker-stable \
            zap-baseline.py \
            -t http://localhost:3000 \
            -r zap-report.html \
            || echo "Scan terminé (même avec des erreurs)"

      - name: 📤 Upload HTML Report
        uses: actions/upload-artifact@v4
        with:
          name: dast-report
          path: juice-shop/zap-report.html
          retention-days: 7

      - name: 🧹 Cleanup Juice Shop
        run: |
          pkill -f "node .*juice-shop"
          echo "Nettoyage terminé"
